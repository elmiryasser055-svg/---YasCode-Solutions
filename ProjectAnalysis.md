# ProjectAnalysis.md
## نظام تسيير محلات المواد الغذائية (Supérette Management System)
**YasCode Solutions — تحليل المرحلة 1**

---

## 1. ملخص تنفيذي

تطبيق **Desktop offline-first** مبني بـ Electron، موجّه لأصحاب محلات المواد الغذائية (supérettes)، يوفّر نظام **POS (نقطة بيع) متكامل** يغطي: البيع عبر قارئ الباركود، إدارة المخزون بدقة (كمية، تاريخ صلاحية، حركة كل منتج)، إدارة المشتريات والموردين، صندوق نقدي (cash register) مع تسوية يومية، تقارير أرباح، ونظام صلاحيات بمستويين (Owner / Cashier).

النظام لا يعتمد على أي اتصال إنترنت للعمل، يخزّن كل البيانات محليًا، ويجب أن يتحمّل انقطاع الكهرباء المفاجئ دون فقدان بيانات — وهذا يوجّه عدة قرارات تقنية أدناه (خصوصًا اختيار قاعدة البيانات وطريقة الكتابة عليها).

**نوع المستخدم:** مستخدم واحد أساسي (Owner) + إمكانية حسابات Cashier لاحقًا بصلاحيات محدودة. لا يوجد تعدد فروع، لا مزامنة سحابية، لا حاجة لـ scalability بالمعنى التقليدي (عدد مستخدمين قليل جدًا وثابت) — لكن الحاجة الحقيقية هنا هي **موثوقية البيانات والسرعة في العمليات اليومية المتكررة** (كل عملية بيع، كل مسح باركود).

---

## 2. الميزات مقسّمة حسب الأولوية

### 🔴 Core Features (لا إطلاق بدونها)

**المبيعات ونقطة البيع**
- إدخال المنتجات يدويًا + عبر قارئ باركود (كـ keyboard wedge/HID)
- دعم منتجات بدون باركود (بحث بالاسم)
- دعم بيع بالقطعة وبالوزن
- طباعة تذكرة البيع (ticket) على طابعة USB حرارية ESC/POS
- إعادة طباعة أي فاتورة سابقة
- إلغاء/تعديل عملية بيع (مع سجل تدقيق audit trail)
- اختصارات لوحة مفاتيح لتسريع عملية البيع

**المخزون (Inventory)**
- تعديل الكمية، stock in / stock out / inventory adjustment
- تنبيهات low stock
- تتبّع تاريخ الصلاحية: منتجات منتهية + منتجات ستنتهي قريبًا (قابل للتهيئة، مثلاً خلال 30 يوم)
- معرفة كمية أي منتج لحظيًا
- **سجل كامل لكل حركة مخزون (stock movement log)** — غير قابل للتعديل، فقط إضافة (append-only)

**المشتريات والموردون**
- قائمة الموردين، ديون الموردين
- فواتير الشراء، تاريخ المشتريات، تكلفة الشراء لكل منتج

**الصندوق (Cash Register)**
- إجمالي المبيعات، المرتجعات، المصاريف
- المبلغ المتوقع في الصندوق مقابل المبلغ الفعلي (جرد يومي) + الفرق
- فتح/إغلاق جلسة صندوق (session) لكل يوم عمل

**الباركود**
- توليد باركود للمنتجات التي لا تملك واحدًا
- طباعة labels
- قراءة وبحث بالباركود

**النظام**
- تخزين محلي بالكامل + عمل offline كامل
- نظام صلاحيات: Owner (كامل) / Cashier (بيع فقط، بدون تقارير مالية أو تعديل أسعار)

### 🟡 Nice-to-have (لاحقًا)
- تقارير ورسوم بيانية للأرباح (يومي/أسبوعي/شهري) — **تم اعتمادها من طرفك، ستُضاف في نسخة أولى مبسطة ضمن Core فعليًا لأنها تعتمد على بيانات موجودة أصلاً، لكن التصميم التفصيلي للرسوم البيانية سيُفصّل أكثر في مرحلة الفرونت-إند**
- دعم لغتين: عربي/فرنسي (i18n) — معتمدة، ستُبنى من البداية في هيكلة الفرونت-إند حتى لا تكون إعادة هيكلة لاحقة مكلفة
- ديون الزبائن (لم تُذكر كـ Core، ستُضاف كموديول منفصل لاحقًا إن رغبت)
- نسخ احتياطي تلقائي محلي (سيُضاف كخدمة خلفية بسيطة، أولوية متوسطة لأنه يحمي البيانات — أنصح بعدم تأجيله كثيرًا)
- دعم multi-branch (غير مطلوب حاليًا، لكن سنتجنب قرارات تصميم في السكيمة (Phase 2) تمنعه لاحقًا إن أردت التوسع)

---

## 3. الأدوات والتقنيات المقترحة

### 3.1 الإطار العام: Electron ✅ (تأكيد اختيارك)
**السبب:** وصول كامل لـ Node.js APIs (نظام ملفات، SQLite، طابعات، منافذ USB/Serial)، عمل offline طبيعي 100%، ونضج كبير في هذا النوع بالذات من التطبيقات (POS/إدارة محلات).
**البديل المرفوض:** Tauri — أخف وأداء أفضل، لكن التكامل مع مكتبات طباعة ESC/POS وقراءة باركود الجاهزة في نظام Node أضعف بكثير حاليًا، وسيتطلب كتابة طبقات Rust مخصصة. غير مبرر هنا.

### 3.2 قاعدة البيانات: SQLite (عبر `better-sqlite3`)
**السبب:** قاعدة بيانات محلية، ملف واحد، لا تحتاج خادم منفصل، سريعة جدًا للعمليات المتزامنة (synchronous API) وهو أمر مهم في تطبيق POS حيث لا نريد أي "انتظار" غير ضروري عند كل عملية بيع. دعم **WAL mode (Write-Ahead Logging)** يحمي من فقدان البيانات عند انقطاع الكهرباء المفاجئ — وهذا **قرار حاسم** بالنظر لطبيعة الاستخدام (محل قد ينقطع فيه التيار).

### 3.3 ⚠️ ORM: نقترح **Drizzle ORM** بدل Prisma — قرار يحتاج موافقتك

خطتك الأصلية تفترض `schema.prisma` في المرحلة 2. أطرح عليك بديلاً مبرَّرًا قبل أن نلتزم:

| المعيار | Prisma | Drizzle ORM |
|---|---|---|
| التوافق مع Electron المُعبَّأ (packaged app) | يحتاج تهيئة إضافية لتضمين query engine binary لكل نظام تشغيل (مصدر شائع لمشاكل عند التوزيع) | لا يحتوي محرك خارجي، كود TypeScript خالص يُبنى مباشرة مع التطبيق — لا مشاكل تعبئة |
| الأداء مع better-sqlite3 | جيد لكن عبر طبقة إضافية | تكامل مباشر وخفيف مع better-sqlite3 (sync) |
| الـ Migrations | نظام migrate خاص به، قوي لكن أثقل | ملفات SQL migration بسيطة وواضحة، أسهل تتبعًا في مشروع صغير/متوسط |
| منحنى التعلم | أعلى قليلاً | أبسط، أقرب لـ SQL خام مع type-safety |

**توصيتي:** استخدام **Drizzle** لتفادي مشاكل تعبئة Prisma الشائعة في تطبيقات Electron الموزّعة (exe/installer)، خصوصًا أن المشروع لن يحتاج ميزات Prisma المتقدمة (لا حاجة لعدة قواعد بيانات، لا حاجة لـ Prisma Studio في الإنتاج). إذا كانت لديك تجربة سابقة مع Prisma وتفضّل الاستمرار به، هذا ممكن تمامًا (فقط سنحتاج تهيئة `asarUnpack` بعناية في `electron-builder`) — أخبرني بقرارك وسأثبّته في المرحلة 2.

### 3.4 الفرونت-إند (Renderer Process)
- **React + TypeScript** — الأنسب لتطبيق بهذا الحجم من التفاعل (شاشات بيع، جداول، فورمات كثيرة)
- **TailwindCSS + shadcn/ui** — بناء واجهة احترافية وسريعة دون إعادة اختراع مكونات (جداول، modals، forms)
- **Zustand** لإدارة الحالة — أخف من Redux وكافٍ تمامًا لتطبيق desktop بمستخدم واحد فعليًا في كل لحظة (لا حاجة لتعقيد Redux Toolkit)
- **react-i18next** لدعم عربي/فرنسي مع دعم RTL/LTR ديناميكي (مهم جدًا لأن العربية RTL والفرنسية LTR)
- **recharts** للرسوم البيانية (تقارير الأرباح)

### 3.5 الباك-إند (Main Process — منطق داخل نفس تطبيق Electron)
- **Node.js + TypeScript** ضمن الـ main process
- **better-sqlite3 + Drizzle** كما ذُكر
- **bwip-js** لتوليد الباركود (يدعم EAN-13, CODE128... كصورة/buffer)
- **node-thermal-printer** أو **escpos** (npm) للتواصل مع الطابعة الحرارية عبر USB وطباعة التذاكر وlabels الباركود (نفس الطابعة كما أكدت) — سنقارن الاثنين عمليًا في مرحلة الباك-إند ونختار الأنسب حسب توافقهما مع نظام التشغيل المستهدف (Windows على الأغلب)
- **قارئ الباركود:** لا يحتاج مكتبة خاصة — يعمل كـ HID keyboard، سنبني معالج إدخال (input buffer) يفرّق بين الكتابة اليدوية ومسح الباركود اعتمادًا على سرعة الإدخال (timing-based detection)

### 3.6 التعبئة والتوزيع
- **electron-builder** لإنشاء installer (خصوصًا لـ Windows، النظام الأكثر شيوعًا في هذا السياق) — سنؤكد نظام التشغيل المستهدف في مرحلة النشر

---

## 4. المخاطر التقنية المحتملة

| الخطر | التأثير | التخفيف المقترح |
|---|---|---|
| انقطاع الكهرباء أثناء الكتابة على القاعدة | فقدان/تلف بيانات بيع حرجة | SQLite WAL mode + transactions صارمة على كل عملية بيع/حركة مخزون |
| عدم وجود نسخ احتياطي فعلي | فقدان كامل للبيانات عند تلف الجهاز | تصميم آلية backup محلي دوري من الآن (حتى كـ nice-to-have، أنصح بعدم تأجيلها لمرحلة متأخرة جدًا) |
| اختلاف سلوك قارئ الباركود بين الأجهزة | مسح خاطئ أو بطيء | معالج إدخال قابل للتهيئة (delay threshold قابل للتعديل من الإعدادات) |
| مشاكل تعبئة ORM في نسخة exe نهائية | تعطل التطبيق بعد التثبيت رغم عمله في dev | استخدام Drizzle (يتفادى المشكلة) أو تهيئة دقيقة لـ Prisma إن أُصرّ عليه |
| تعدد اللغات مع RTL/LTR | أخطاء تخطيط (layout) إن لم تُبنَ من البداية | بناء دعم i18n + اتجاه الصفحة من أول سطر كود في الفرونت-إند، وليس كإضافة لاحقة |
| نمو حجم قاعدة بيانات الحركات (stock movement log) مع الوقت | بطء تدريجي في الاستعلامات | فهرسة (indexes) صحيحة على الحقول المستخدمة في البحث/الفلترة (تاريخ، معرف المنتج) منذ تصميم السكيمة |

---

## 5. خارطة طريق أولية للمراحل القادمة

1. **المرحلة 2 — تصميم قاعدة البيانات:** تحديد نهائي لـ ORM (Drizzle أم Prisma)، ثم بناء `schema.prisma`/schema Drizzle و`schemaDB.md` تغطي: المنتجات، الفئات، حركات المخزون، المبيعات وبنودها، الموردين والمشتريات، جلسات الصندوق، المستخدمين والأدوار
2. **المرحلة 3 — الباك-إند:** بنية `lib/`, `middleware/` (تُكيَّف لسياق Electron main process بدل REST تقليدي)، `modules/` لكل نطاق (sales, inventory, purchases, cash-register, printing, barcode)
3. **المرحلة 4 — الفرونت-إند:** شاشة POS رئيسية، شاشات إدارة (منتجات/مخزون/موردين)، شاشة صندوق، شاشة تقارير، دعم i18n كامل
4. **المرحلة 5 — النشر:** تعبئة exe عبر electron-builder، استراتيجية نسخ احتياطي، لا حاجة لـ CI/CD معقد (توزيع محلي مباشر على جهاز واحد على الأرجح)

---

## 6. افتراضات تم اعتمادها (بانتظار تصحيحك إن لزم)

- لا يوجد multi-branch حاليًا
- لا حاجة لاتصال إنترنت دائم؛ أي تحديثات مستقبلية للتطبيق ستكون يدوية
- نظام تشغيل مستهدف مبدئيًا: **Windows** (الأكثر شيوعًا لهذا السياق) — يُرجى التأكيد أو التصحيح
- ديون الزبائن غير مطلوبة في النسخة الأولى

---

## 7. قرارات معتمدة نهائيًا ✅

- **ORM المعتمد: Drizzle ORM** (مع `better-sqlite3`)
- **نظام التشغيل المستهدف: Windows فقط**

المرحلة 1 مغلقة رسميًا. الانتقال للمرحلة 2 (تصميم قاعدة البيانات).
