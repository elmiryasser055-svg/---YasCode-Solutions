# schemaDB.md
## شرح سكيمة قاعدة البيانات — نظام تسيير محلات المواد الغذائية
**YasCode Solutions — المرحلة 2**

ORM: Drizzle | DB: SQLite (`better-sqlite3`, WAL mode مفعّل)

---

## 1. الجداول والغرض من كل واحد

### `users`
المستخدمون: Owner واحد على الأقل + Cashiers اختياريين. `role` يحدد الصلاحيات على مستوى التطبيق (المرحلة 3 — middleware التفويض). `isActive` بدل الحذف الفعلي، حتى تبقى سجلات المبيعات القديمة مرتبطة بمستخدم موجود تاريخيًا حتى لو عُطّل لاحقًا.

### `categories` / `products`
تصنيف بسيط (فئة واحدة لكل منتج، كافٍ لحجم محل). `products.currentQuantity` هو **الحقل السريع** الذي تقرأه واجهة البيع مباشرة (بدون حساب SUM في كل عملية بيع)، ويُحدَّث **حصريًا** ضمن نفس transaction مع إضافة سجل في `stock_movements` — هذا يضمن تطابق الاثنين دائمًا. `barcode` نصّي و**قابل للـ null** (منتجات بدون باركود)، لكن مفهرس (`unique index`) لمنع تكرار نفس الباركود بين منتجين.

### `stock_movements`
**مصدر الحقيقة الكامل** لتاريخ أي منتج. append-only (لا تعديل ولا حذف من واجهة التطبيق). كل عملية تؤثر على الكمية — بيع، إلغاء بيع، إرجاع، شراء، تعديل جرد يدوي — تُنشئ سطرًا هنا مع `quantityChange` (موجب أو سالب) و`quantityAfter` (الرصيد اللحظي بعدها، يسهّل التدقيق دون إعادة حساب). هذا يحقق مباشرة متطلب "سجل كل حركة على المخزون".

### `cash_register_sessions` / `cash_movements` / `expenses`
الصندوق يُبنى حول **جلسة يومية**: تُفتح في بداية الدوام بمبلغ ابتدائي (`openingAmount`)، تتراكم عليها `cash_movements` (كل بيع نقدي، كل مصروف، كل إرجاع)، ثم تُغلق بحساب `expectedAmount` تلقائيًا من مجموع الحركات، مقابل `actualAmount` (العدّ اليدوي)، والفرق `difference` محسوب ومخزّن (وليس فقط في الواجهة) حتى يبقى في التقارير التاريخية. `expenses` منفصل عن `cash_movements` من ناحية البيانات الوصفية (فئة المصروف) لكنه يُسجَّل أيضًا كحركة صندوق.

### `sales` / `sale_items`
كل عملية بيع لها `saleNumber` تسلسلي يُطبع على التذكرة (منفصل عن `id` الداخلي، لأنك قد تحتاج لاحقًا تصفير الترقيم سنويًا مثلاً). **أهم قرار هنا:** `sale_items` يخزّن **snapshot** لاسم المنتج، الباركود، سعر البيع، **وسعر التكلفة وقت البيع** (`costPriceSnapshot`). هذا ضروري لأن سعر الشراء يتغيّر بمرور الوقت، وحساب "الربح" لفاتورة قديمة يجب أن يعتمد على التكلفة الفعلية وقتها لا التكلفة الحالية — وإلا كانت تقارير الأرباح التاريخية غير دقيقة. `productId` قابل للـ null (`onDelete: set null`) حتى لو حُذف المنتج مستقبلاً، الفاتورة التاريخية تبقى قابلة لإعادة الطباعة كاملة عبر الـ snapshot.

### `returns`
مرتبط بـ `sale_items` مباشرة (وليس بـ `sales` ككل) لأن الإرجاع غالبًا جزئي (منتج واحد من فاتورة متعددة المنتجات).

### `suppliers` / `purchases` / `purchase_items` / `supplier_payments`
`purchases.status` (`paid/partial/unpaid`) محسوب ويُحدَّث عند كل `supplier_payment` جديد، لكنه مخزَّن (وليس محسوبًا فقط عند القراءة) لتسريع شاشة "قائمة الفواتير غير المدفوعة". **دَين المورد الإجمالي** = `SUM(purchases.totalAmount) - SUM(supplierPayments.amount)` لكل مورد — يُحسب عند الطلب (query) وليس حقلاً مخزَّنًا، لأنه مجموع بسيط لا يستدعي caching في هذا الحجم من البيانات.

### `audit_log`
يغطي ما لا تغطيه `stock_movements`: **تعديل سعر منتج** (مطلوب صراحة في المرحلة 1: "الغاء وتعديل عملية البيع")، إلغاء/تعديل بيع (بالإضافة لحقول `cancelledBy/cancelReason` المباشرة على `sales`)، وإدارة المستخدمين. `oldValue`/`newValue` كنص JSON عام حتى لا نحتاج جدول منفصل لكل نوع تغيير.

### `app_settings`
جدول عام key/value لإعدادات مثل: عتبة "قريب من الانتهاء" بالأيام، اللغة الافتراضية، اسم الطابعة المختارة — بدل حقول ثابتة تحتاج migration في كل مرة تُضاف فيها إعداد جديد.

---

## 2. العلاقات الأساسية ولماذا

- **`products` → `category` (many-to-one):** منتج واحد بفئة واحدة، كافٍ لهذا الحجم؛ لا حاجة لعلاقة many-to-many بين منتجات وفئات.
- **`products` → `stock_movements` (one-to-many):** كل حركة مخزون تخص منتجًا واحدًا فقط.
- **`sales` → `sale_items` (one-to-many) مع `cascade` عند الحذف:** حذف بيع (نظريًا، لا يُتاح من الواجهة عادة) يحذف بنوده تلقائيًا — لكن عمليًا سنعتمد **الإلغاء المنطقي** (`status: cancelled`) لا الحذف الفعلي، حفاظًا على السجل التاريخي والصندوق.
- **`purchases` → `purchase_items` (one-to-many):** بنود فاتورة الشراء، تُستخدم لتحديث `products.currentQuantity` (زيادة) و`stock_movements` (نوع `purchase`) عند تأكيد الفاتورة.
- **`suppliers` → `purchases` و`suppliers` → `supplier_payments` (one-to-many):** لحساب الدَين الإجمالي وتاريخ التعاملات مع كل مورّد.
- **`cash_register_sessions` → `sales` و`→ cash_movements` (one-to-many):** كل بيع يجب أن يحدث ضمن جلسة صندوق مفتوحة (قيد منطقي يُفرض في طبقة الـ service بالمرحلة 3، وليس عبر SQL constraint، لأن SQLite لا يدعم شروطًا شرطية معقدة في FK).

---

## 3. سيناريوهات استخدام حقيقية (Use Cases)

**سيناريو 1 — عملية بيع كاملة:**
الكاشير يمسح باركود منتج → يُبحث عنه في `products` → يُضاف كسطر في سلة العرض (ذاكرة الواجهة فقط، ليس DB بعد) → عند "إتمام البيع": ضمن **transaction واحدة**: (أ) إنشاء سطر `sales`، (ب) إنشاء أسطر `sale_items` بكل الـ snapshots، (ج) لكل منتج: تحديث `products.currentQuantity` وإضافة سطر `stock_movements` (type: sale)، (د) إضافة سطر `cash_movements` (type: sale) بقيمة الإجمالي. فشل أي خطوة يُلغي كل شيء (rollback) — لا يمكن أبدًا أن تُخصم كمية بدون فاتورة مطابقة.

**سيناريو 2 — إلغاء بيع بعد إتمامه:**
تحديث `sales.status = cancelled` + `cancelledBy` + `cancelReason` → لكل `sale_item`: إعادة الكمية عبر سطر جديد في `stock_movements` (type: `sale_cancel`, quantityChange موجب) → تحديث `products.currentQuantity` → إضافة سطر `cash_movements` سالب لعكس أثر الصندوق → سطر في `audit_log` (action: `sale_cancel`). **لا حذف فعلي لأي سجل.**

**سيناريو 3 — تنبيه انتهاء الصلاحية:**
استعلام دوري (عند فتح شاشة المخزون أو يوميًا): `SELECT * FROM products WHERE expiryDate BETWEEN NOW AND NOW + settings.expiryWarningDays` (منتجات قريبة) و`WHERE expiryDate < NOW` (منتهية بالفعل) — الفهرس `products_expiry_idx` يجعل هذا الاستعلام سريعًا حتى مع آلاف المنتجات.

**سيناريو 4 — تسوية الصندوق في نهاية اليوم:**
عند "إغلاق الجلسة": `expectedAmount = openingAmount + SUM(cash_movements حيث type IN sale) - SUM(حيث type IN expense, return) ± manual_in/out` → الكاشير/المالك يُدخل `actualAmount` بعد عدّ النقد يدويًا → `difference` يُحسب ويُخزَّن → الجلسة تُقفل نهائيًا (لا تعديل لاحق، فقط عرض).

**سيناريو 5 — فاتورة شراء جديدة من مورّد:**
إدخال `purchases` (حتى لو غير مدفوعة بالكامل: `status: unpaid/partial`) → `purchase_items` لكل منتج مع `unitCost` الفعلي في هذه الفاتورة → **تحديث `products.purchasePrice`** للسعر الجديد (يؤثر على المبيعات القادمة فقط، بفضل الـ snapshot في `sale_items`) → زيادة `currentQuantity` + سطر `stock_movements` (type: purchase).

---

## 4. الحالات الحدّية (Edge Cases) وكيف تعالجها السكيمة

| الحالة | المعالجة |
|---|---|
| بيع منتج بدون باركود | `products.barcode = NULL`، البحث في الواجهة يعتمد على `name` (مفهرس) |
| حذف منتج له فواتير بيع قديمة | `sale_items.productId` بـ `onDelete: set null` — الفاتورة تبقى كاملة عبر الـ snapshots حتى لو اختفى المنتج من الجدول الأساسي |
| تغيّر سعر البيع/التكلفة بعد صدور فواتير | لا يؤثر على أي فاتورة سابقة إطلاقًا بفضل `unitPrice`/`costPriceSnapshot` في `sale_items` |
| بيع كمية أكبر من المخزون المتوفر | يُمنع في طبقة الـ service (المرحلة 3) قبل الوصول لقاعدة البيانات؛ السكيمة نفسها لا تفرض `CHECK (currentQuantity >= 0)` تحسبًا لحالات تصحيح جرد استثنائية يدوية من المالك |
| انقطاع الكهرباء أثناء عملية بيع | بفضل SQLite WAL + transaction واحدة (كل خطوات البيع سويًا)، إمّا تُنفَّذ كل الكتابات أو لا شيء — لا حالة "بيع منتصف منفّذ" |
| فاتورة شراء بدون رقم فاتورة (مورّد لا يصدر فواتير رسمية) | `invoiceNumber` قابل للـ null |
| منتج بدون تاريخ صلاحية (مثل أدوات تنظيف) | `expiryDate` قابل للـ null، يُستثنى تلقائيًا من استعلامات تنبيهات الانتهاء |
| مستخدم Cashier يُعطَّل لكنه أنشأ مبيعات سابقة | `isActive = false` بدل الحذف، الفواتير التاريخية تبقى مرتبطة بـ `userId` صالح |

---

## 5. استراتيجية الـ Migrations

المشروع صغير/متوسط الحجم ومتوقع تغييرات دورية بسيطة (إضافة حقل، جدول جديد لميزة nice-to-have لاحقًا مثل ديون الزبائن). نعتمد:

- **Drizzle Kit** لتوليد ملفات migration بصيغة SQL خام تلقائيًا من التغييرات في `schema.ts` (`drizzle-kit generate`)
- كل migration يُحفظ كملف مرقّم ضمن مجلد `drizzle/migrations/` ويُطبَّق تلقائيًا عند إقلاع التطبيق (باستخدام `drizzle-orm/better-sqlite3/migrator`) — بما يضمن أن أي تحديث للتطبيق (نسخة exe جديدة) يُحدّث قاعدة بيانات المستخدم محليًا دون تدخل يدوي منه
- **نسخة احتياطية تلقائية من ملف قاعدة البيانات قبل تطبيق أي migration** عند الإقلاع (نسخ الملف `.sqlite` بامتداد `.bak-{timestamp}` قبل التحديث) — طبقة أمان إضافية ضرورية بما أن لا يوجد مستخدم تقني يتدخل يدويًا عند حدوث خطأ
