# RUNNING.md
## كيف تُشغّل المشروع محليًا

هذا الملف عملي بحت (وليس جزءًا من مخرجات المراحل الخمس الرسمية) — يشرح كيف تُشغّل كل ما بُني عبر المراحل 1-5 على جهازك.

---

## 0. المتطلبات قبل البدء

- **Node.js** إصدار 20 أو أحدث ([nodejs.org](https://nodejs.org))
- **Git** (اختياري، فقط إن أردت رفع المشروع لمستودع)
- **Python + أدوات بناء C++** (مطلوبة لتصريف `better-sqlite3` محليًا):
  - Windows: `npm install --global windows-build-tools` أو تثبيت "Desktop development with C++" من Visual Studio Build Tools
- إن كان لديك طابعة ESC/POS حقيقية للاختبار، وصّلها عبر USB قبل التشغيل (اختياري في مرحلة التطوير)

---

## 1. فك الضغط وترتيب الملفات

فُك ضغط `YasCode-Supperette-Project.zip`. الهيكلة النهائية يجب أن تكون:

```
YasCode-Supperette-Project/
├── package.json          ← ✅ جديد (يجمع كل شيء، أضفته الآن لتشغيل المشروع فعليًا)
├── vite.config.mts          ← ✅ جديد (الامتداد .mts إجباري — راجع التعليق داخل الملف)
├── tsconfig.json             ← ✅ جديد
├── tsconfig.electron.json      ← ✅ جديد
├── tailwind.config.js            ← ✅ جديد
├── postcss.config.js               ← ✅ جديد
├── drizzle.config.ts                 ← ✅ جديد
├── .gitignore                          ← ✅ جديد
├── db/schema.ts                (من المرحلة 2)
├── backend/                      (من المرحلة 3)
├── frontend/
│   ├── index.html                ← ✅ جديد
│   ├── electron/
│   │   ├── main.ts                ← ✅ جديد (كان ناقصًا)
│   │   └── preload.ts               (من المرحلة 4)
│   └── src/                            (من المرحلة 4)
└── deploy/                                (من المرحلة 5 — package.json هناك أصبح مرجعيًا فقط، الفعلي في الجذر الآن)
```

> **لماذا أُضيفت ملفات جديدة الآن؟** المراحل 1-5 أنتجت المنطق والبنية (السكيمة، الباك-إند، الشاشات، خطة النشر) — لكن "ملفات التوصيل" (نقطة إقلاع Electron الفعلية `main.ts`، إعداد Vite، إعداد Tailwind...) لم تكن مطلوبة صراحة في أي مرحلة، فلم تُبنَ إلا الآن لجعل المشروع قابلاً للتشغيل الفعلي.

---

## 2. تثبيت التبعيات

من داخل مجلد المشروع:

```bash
npm install
```

هذا سيُشغّل تلقائيًا `postinstall` الذي يُعيد تصريف `better-sqlite3` ليتوافق مع Electron (خطوة إجبارية، راجع `Deploy.md` § 1).

---

## 3. توليد ملفات الـ migrations لأول مرة

السكيمة (`db/schema.ts`) موجودة، لكن ملفات SQL الفعلية (`drizzle/migrations/`) تُولَّد مرة واحدة عبر:

```bash
npm run db:generate
```

سيظهر مجلد جديد `drizzle/migrations/` يحتوي ملف SQL أول. هذا الملف يُطبَّق تلقائيًا عند أول تشغيل للتطبيق (`initDatabase()` في `backend/lib/db.ts`، المرحلة 3) — لا حاجة لأي أمر إضافي لتطبيقه يدويًا.

---

## 4. تشغيل وضع التطوير

```bash
npm run dev
```

هذا الأمر الواحد يُشغّل 3 أشياء معًا (عبر `concurrently`):
1. خادم Vite للواجهة (renderer) على `http://localhost:5173`
2. تصريف مستمر (`watch`) لملفات Electron/الباك-إند
3. فتح نافذة Electron فعليًا بعد جاهزية الاثنين

**أول مستخدم:** قاعدة البيانات ستكون فارغة تمامًا عند أول تشغيل (لا يوجد حساب Owner افتراضي مبني مسبقًا عمدًا). شغّل سكربت الـ seed **قبل** فتح التطبيق لأول مرة:

```bash
npm run seed:owner -- admin "MyStrongPass123" "صاحب المحل"
```

⚠️ **مهم جدًا حول `better-sqlite3`:** هذه مكتبة C++ مُصرَّفة، ولكل "مُضيف" (Node النظام العادي، أو Node المدمج داخل Electron) إصدار ABI مختلف تمامًا. `npm run seed:owner` و`npm run dev` **يُعيدان تصريفها تلقائيًا** كل مرة (عبر `predev`/داخل سكربت `seed:owner` نفسه) لتفادي خطأ شائع من نوع:
```
NODE_MODULE_VERSION 128... requires NODE_MODULE_VERSION 127
```
هذا يعني أن كل تشغيل لأي منهما يستغرق ثوانٍ إضافية لإعادة التصريف — طبيعي ومتوقع، وليس خطأً. **لا تشغّل `tsx scripts/seed-owner.ts` مباشرة بلا المرور بـ `npm run seed:owner`**، وإلا ستحصل على نفس خطأ عدم التطابق.

**إن واجهت مشكلة في `npm run dev`** (تزامن الأوامر الثلاثة أحيانًا حسّاس للتوقيت أول مرة)، شغّلها يدويًا في نافذتي طرفية منفصلتين كبديل أضمن:
```bash
# نافذة 1
npm run dev:renderer

# نافذة 2 (بعد ظهور "VITE ready" في النافذة 1)
npm run dev:electron
# ثم افتح نافذة ثالثة بعد أول تصريف ناجح:
npx cross-env NODE_ENV=development electron .
```

---

## 5. تشغيل الاختبارات

```bash
npm run test:unit     # Vitest workspace — 42 اختبارًا فعليًا (23 باك-إند + 19 فرونت-إند)
npm run test:e2e       # Playwright — مكتوب لكن غير مُشغَّل في بيئة التطوير (راجع playwright.config.ts)
```

`vitest.workspace.ts` (جذر المشروع) يفصل بيئتي اختبار: الباك-إند يعمل بـ `environment: "node"` (قاعدة بيانات `:memory:` حقيقية عبر `initDatabase()` الفعلية)، والفرونت-إند بـ `environment: "jsdom"` (مكوّنات React بـ `@testing-library/react`). هذه الاختبارات كشفت فعليًا خطأين حرجين موثَّقين في `back-end.md` § 8، وأثبتت فعليًا (وليس نظريًا) أن الروابط الحسّاسة تختفي من الواجهة عن حساب Cashier.

**بخصوص `npm run test:e2e`:** الاختبار مكتوب بصيغة صحيحة حسب توثيق Playwright الرسمي، لكن لم يُشغَّل فعليًا أثناء بناء هذا المشروع (البيئة المستخدمة بلا شاشة عرض قادرة على تشغيل نافذة Electron). شغّله بنفسك بعد `npm run predist` للتحقق قبل الاعتماد عليه بنفس ثقة اختبارات Vitest.

---

## 6. بناء نسخة exe تجريبية (Windows)

```bash
npm run predist
npm run dist:win
```

الناتج: ملف `.exe` داخل `dist-installer/` (راجع `Deploy.md` للتفاصيل الكاملة، بما فيها CI/CD الجاهز في `deploy/.github/workflows/build-release.yml`).

---

## 7. حالة الفجوات (نهائية تمامًا)

**🔴🔴🔴 ثلاثة إصلاحات حرجة تم اكتشافها من التشغيل الفعلي (وليس فقط الاختبارات):**
1. `db.transaction(async...)` لا يعمل مع `better-sqlite3` — كان سيُسقط كل عملية بيع/شراء/إرجاع/مصروف (اكتُشف بالاختبارات)
2. `vite.config.ts` يتعارض مع `vitest.workspace.ts` — أُصلح بتغيير الاسم إلى `vite.config.mts` (اكتُشف بالاختبارات)
3. **مسار `drizzle/migrations` خاطئ بعد التصريف + خطأ TypeScript ممنهج في `withValidation`** — **اكتشفهما تشغيل المستخدم الفعلي لـ `npm run dev`**، ولم تكشفهما أي اختبارات Vitest سابقة (لأن الاختبارات تُشغَّل من المصدر مباشرة بلا خطوة تصريف tsc منفصلة). التفاصيل الكاملة في `backend/back-end.md` § 12.

**هذا يوضّح حدود الاختبارات الآلية:** Vitest يثبت صحة *المنطق*، لكن لا يكتشف بالضرورة أخطاء *خطوة البناء نفسها* (مسارات نسبية تختلف بعد التصريف). لهذا كان تشغيلك الفعلي للمشروع لا يُقدَّر بثمن — كشف فئة كاملة من الأخطاء لم تكن الاختبارات لتكشفها.

**كل الفجوات الوظيفية المعروفة سُدَّت الآن:**

الباك-إند:
- ✅ Pagination حقيقي، استيراد/تصدير CSV، استعادة نسخة احتياطية، قناة `sales:get`
- ✅ `env.ts`/`logger.ts` مستقلان عن Electron، `db.transaction` مُصلح، مسار `migrations` مُصلح، `withValidation` مُصلح

الفرونت-إند:
- ✅ نافذة تأكيد عامة، `CategoriesScreen`، `BackupScreen`، pagination، أزرار CSV
- ✅ تعديل/إلغاء/إرجاع بيع، الطباعة، الإعدادات، الاختصارات، سكربت seed، اختبار E2E مكتوب (غير مُشغَّل فعليًا بعد)

**إجمالي اختبارات Vitest الفعلية الناجحة: 45/45** (24 باك-إند + 19 فرونت-إند + 2 اختبار قواعد التحقق الجديد) — راجع `npm run test:unit`. تحقّق `tsc -p tsconfig.electron.json --noEmit` أيضًا بصفر أخطاء (نفس أمر `build:electron` الحقيقي بالضبط).

**تبقّى فعليًا (بنود تجميلية بسيطة، غير وظيفية):**
- تشغيل فعلي لـ `e2e/login.spec.ts` على جهاز حقيقي بشاشة عرض
- أيقونة احترافية أكثر
- اختبار فعلي للطباعة/الباركود على عتاد حقيقي
- Code Signing لملف exe (اختياري، مذكور في `Deploy.md`)
